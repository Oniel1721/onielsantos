---
const { href, style } = Astro.props as { href: string; style: string };
---

<a style={style} href={href} target="_blank">
  <slot />
</a>

<style is:global>
  a {
    color: var(--secondary);
    display: inline-flex;
    align-items: center;
    font-weight: 600;
  }
</style>
